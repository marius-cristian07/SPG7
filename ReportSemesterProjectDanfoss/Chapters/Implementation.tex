This chapter explains how the system was technically designed,
implemented, and improved during the project. Its purpose is not only to
show what was built, but also how the group worked technically and why
specific engineering choices were made.

% --------------------------------
\section{Frontend Implementation}


The frontend was implemented using the Avalonia UI framework together with the MVVM (Model--View--ViewModel) architectural pattern. The purpose of the frontend was to provide operators and users with an intuitive interface for selecting optimization scenarios, and visualizing optimization results in a structured and user-friendly way.

The implementation focused on separating the UI layer from the Business layer and Data layer in order to improve scalability, and code readability. 

\subsection{Frontend Architecture}

The frontend was structured into multiple ViewModels and Views:

\begin{itemize}
    \item \texttt{MainViewModel.cs} (MainWindow.axaml)
    \item \texttt{AssetViewModel.cs} (AssetView.axaml)
    \item \texttt{DashboardViewModel.cs} (DashboardView.axaml)
    \item \texttt{ResultViewModel.cs} (ResultView.axaml)
\end{itemize}
Each ViewModel was also connected to the corresponding Avalonia XAML view.
This separation ensured that UI components handled presentation only, while the ViewModels managed application state, navigation, and interaction logic.

\subsection{Main Navigation and View Handling}

The MainViewModel served as the central controller for navigation between different pages of the application. 

The ViewModel maintained a CurrentViewModel property, which determined which page was displayed in the main content area. When the user selected a different tab in the navigation menu, the Navigate() method updated the active ViewModel accordingly.

The tabs were stored using the currently selected tab through the SelectedTab property. The boolean properties:

\begin{itemize}
    \item IsAssetTabSelected
    \item IsDashboardTabSelected
    \item IsResultTabSelected
\end{itemize}
were used to control UI styling and provide visual feedback

\subsection{Asset Management Interface}

The Asset Management page was implemented to allow users to:

\begin{itemize}
    \item See details of production units
    \item Change between scenaios 
    \item Put units into maintenance
\end{itemize}
The AssetViewModel was used to put units into maintenance which was later used by the Domain layer to remove them form the optimization for the given time.

\subsection{Data Interface}

The Data page provided users with a high-level overview of system information and operational data. The purpose of this page was to give quick access to key system metrics before running optimization calculations.

The dashboard implementation focused on clarity and readability through showing only key information about units


\subsection{Result Visualization Interface}

The Result page is used to visualize the optimization output generated by the backend system.

The implementation included interactive graphs for:

\begin{itemize}
    \item Heat production
    \item Heat demand
    \item CO2 emissions
    \item Net production cost
    \item Electricity price (For scenario 2)
\end{itemize}
The graphs were designed to help users quickly identify production patterns and compare optimization results across different scenarios.

The interface included:

\begin{itemize}
    \item Scenario selection buttons
    \item Winter/Summer selection buttons
    \item Dynamic graphs
    \item Legends for graphs
\end{itemize}
The graph visualization clearly distinguished between different heat production units using colors and layered chart areas.

\subsection{Communication Between Frontend and Backend}

The frontend communicated with backend through the ViewModels. The UI itself did not directly perform optimization calculations instead it sent information user selected to the backend.

When the user navigated to the Result page, the MainViewModel collected:

\begin{itemize}
    \item Selected scenario
    \item Selected season
    \item Selected units (Units of the selected scenario)
\end{itemize}

These values were then passed into the ResultViewModel constructor:

\begin{verbatim}
CurrentViewModel = new ResultViewModel(
    AssetPage,
    scenario,
    isSummer,
    selectedUnits
);
\end{verbatim}
This implementation ensured loose coupling between interface components and backend services.



% --------------------------------
\section{Backend Implementation}

Describe how the backend was implemented. Cover:

\begin{itemize}
    \item Main components, classes, and responsibilities.
    \item Handling of business logic.
    \item API, controllers, and services (if relevant).
    \item Data processing and validation.
    \item Communication with the database or persistence layer
          (if relevant).
\end{itemize}

% --------------------------------
\section{Data Management}

Explain how data was modelled and managed. Cover:

\begin{itemize}
    \item Data structure.
    \item Data validation and consistency considerations.
    \item How the data structure integrates with the backend
          (if relevant).
\end{itemize}